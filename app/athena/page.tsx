import ShowaDesk from './ShowaDesk';

export const metadata = {
  title: 'Athena',
  description: 'Focus desk — timer, todos, health.',
};

export default function ToolkitPage() {
  return <ShowaDesk />;
}
