import GameLoader from './components/GameLoader';

export const metadata = {
  title: 'Duck Duck Goose',
  description: 'Ranked Duck Duck Goose.',
};

export default function DuckDuckGoosePage() {
  return <GameLoader />;
}
