import Spritimator from "./Spritimator";

export const metadata = {
  title: "Spritimator",
  description: "Build prompts, map frames, and animate sprite sheets.",
};

export default function SpritimatorPage() {
  return <Spritimator />;
}
